# Example Skill

This is an example skill file. Skills are reusable prompts or instructions
that can be invoked via slash commands in Claude Code.

## Usage

Invoke this skill with `/example` in Claude Code.

## Instructions

Replace this file with your actual skill logic. Each `.md` file in the
`.claude/skills/` directory becomes an available skill command.
